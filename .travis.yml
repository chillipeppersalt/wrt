language: ruby

rvm:
- 3.2.1

before_script:
 - chmod +x ./script/cibuild

branches:
  only:
  - gh-pages     
  - /pages-(.*)/
  - travis

env:
  global:
  - NOKOGIRI_USE_SYSTEM_LIBRARIES=true 

sudo: false
